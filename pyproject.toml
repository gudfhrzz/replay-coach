[project]
name = "replay-coach"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "demoparser2>=0.41.4",
    "pandas>=3.0.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
